<h1>Eat'a'Burger!</h1>

<div id="headerImage">
  {{!-- space for picture --}}
</div>

<h2>Burgs for eaten!</h2>

<ul>
  {{#each cats}}
    {{#unless eaten}}
      {{> cats/cat-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgs that been ate!</h2>

<ul>
  {{#each cats}}
    {{#if eaten}}
      {{> cats/cat-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="eaten">eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> eaten!<br>
    <input type="radio" name="eaten" value="0"> Awake
  </div> --}}

  <button type="submit">Add the Burg</button>
</form>
