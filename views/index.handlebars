
  <div id="title">
    <h1>Eat'a'Burger!</h1>
  </div>

<div id="headerImage">
  {{!-- space for picture --}}
</div>

<h2>Burgs for eaten!</h2>

<ul>
  {{#each cats}}
    {{#unless eaten}}
      {{> cats/burger-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgs that been ate!</h2>

<ul>
  {{#each cats}}
    {{#if eaten}}
      {{> cats/burger-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button id="submit" type="submit">Add the Burg</button>
</form>
